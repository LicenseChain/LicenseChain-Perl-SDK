# LicenseChain Perl SDK Roadmap

## Current Focus

- Keep API base and request behavior aligned with `https://api.licensechain.app/v1`.
- Maintain `verify_with_details` and JWT/JWKS helper compatibility.
- Preserve stable interoperability with current Core API verification contracts.

## Next Operational Tasks

- Re-run conformance checks when verify/JWKS payloads evolve.
- Keep docs pointers and examples aligned with ecosystem docs.
- Track notable updates in `CHANGELOG.md`.
